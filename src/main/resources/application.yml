app:
  name: Webdiet Frontend Service
  version: @project.version@

logging:
  file:
    path: ./logs

route:
  url:
    backend: http://localhost:8090/api

jwt:
  cookie:
    expiration:
      seconds: 3600